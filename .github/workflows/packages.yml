name: Build installers

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  package:
    name: ${{ matrix.name }} installer
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS
            os: macos-13
            bundles: dmg
            artifact: clodx-macos-x64
          - name: macOS Apple Silicon
            os: macos-14
            bundles: app
            artifact: clodx-macos-arm64
            archive_app: true
          - name: Windows
            os: windows-latest
            bundles: msi,nsis
            artifact: clodx-windows
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - run: npm ci
      - run: npm run build
      - run: npm run tauri build -- --bundles ${{ matrix.bundles }}
      - name: Archive Apple Silicon app
        if: matrix.archive_app
        run: >-
          ditto -c -k --sequesterRsrc --keepParent
          src-tauri/target/release/bundle/macos/Clodx.app
          src-tauri/target/release/bundle/macos/Clodx-macos-arm64.app.zip
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: src-tauri/target/release/bundle/**
          if-no-files-found: error
